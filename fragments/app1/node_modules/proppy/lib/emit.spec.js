"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var emit_1 = require("./emit");
describe('proppy :: emit', function () {
    test('is a function', function () {
        expect(typeof emit_1.emit).toEqual('function');
    });
    test('initial state', function () {
        var cancelled = false;
        var P = emit_1.emit(function (cb) {
            cb({ counter: 1 });
            cb({ counter: 2 });
            return function (props) {
                cancelled = props;
            };
        });
        var p = P();
        expect(p.props).toEqual({});
        p.subscribe(function () { });
        expect(p.props).toEqual({ counter: 2 });
        expect(cancelled).toEqual(false);
        p.destroy();
        expect(cancelled).toEqual({ counter: 2 });
    });
});
//# sourceMappingURL=emit.spec.js.map