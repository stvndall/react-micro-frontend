"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/* global describe, test, expect */
var compose_1 = require("./compose");
var withState_1 = require("./withState");
var withHandlers_1 = require("./withHandlers");
describe('proppy :: withHandlers', function () {
    test('is a function', function () {
        expect(typeof withHandlers_1.withHandlers).toEqual('function');
    });
    test('updates', function () {
        var P = compose_1.compose(withState_1.withState('counter', 'setCounter', 0), withHandlers_1.withHandlers({
            increment: function (props) { return function () { return props.setCounter(props.counter + 1); }; },
            decrement: function (props) { return function () { return props.setCounter(props.counter - 1); }; },
        }));
        var p = P();
        expect(p.props.counter).toEqual(0);
        expect(typeof p.props.increment).toEqual('function');
        expect(typeof p.props.decrement).toEqual('function');
        p.props.increment();
        p.props.increment();
        expect(p.props.counter).toEqual(2);
        p.props.decrement();
        expect(p.props.counter).toEqual(1);
    });
});
//# sourceMappingURL=withHandlers.spec.js.map