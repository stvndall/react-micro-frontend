"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/* global describe, test, expect */
var compose_1 = require("./compose");
var withState_1 = require("./withState");
var shouldUpdate_1 = require("./shouldUpdate");
describe('proppy :: shouldUpdate', function () {
    test('is a function', function () {
        expect(typeof shouldUpdate_1.shouldUpdate).toEqual('function');
    });
    test('with state', function () {
        var P = compose_1.compose(withState_1.withState('counter', 'setCounter', 0), shouldUpdate_1.shouldUpdate(function (prevProps, nextProps) { return nextProps.counter % 2 === 0; }));
        var p = P();
        expect(p.props.counter).toEqual(0);
        // shouldn NOT update
        p.props.setCounter(1);
        expect(p.props.counter).toEqual(0);
        // should update
        p.props.setCounter(2);
        expect(p.props.counter).toEqual(2);
    });
});
//# sourceMappingURL=shouldUpdate.spec.js.map