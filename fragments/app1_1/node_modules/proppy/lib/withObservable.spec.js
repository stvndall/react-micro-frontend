"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/* global describe, test, expect */
var withObservable_1 = require("./withObservable");
var rxjs_1 = require("rxjs");
describe('proppy :: withObservable', function () {
    test('is a function', function () {
        expect(typeof withObservable_1.withObservable).toEqual('function');
    });
    test('subscription', function () {
        expect.assertions(2);
        var P = withObservable_1.withObservable(function () { return new rxjs_1.Observable(function (observer) {
            observer.next({
                foo: 'foo value',
            });
            observer.complete();
        }); });
        var p = P();
        expect(p.props).toEqual({});
        p.subscribe(function (props) {
            expect(props).toEqual({
                foo: 'foo value',
            });
        });
    });
});
//# sourceMappingURL=withObservable.spec.js.map