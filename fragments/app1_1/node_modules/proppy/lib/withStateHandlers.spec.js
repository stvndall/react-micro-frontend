"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/* global describe, test, expect */
var withStateHandlers_1 = require("./withStateHandlers");
describe('proppy :: withStateHandlers', function () {
    test('is a function', function () {
        expect(typeof withStateHandlers_1.withStateHandlers).toEqual('function');
    });
    test('updates', function () {
        var P = withStateHandlers_1.withStateHandlers({ counter: 0 }, {
            increment: function (_a) {
                var counter = _a.counter;
                return function () { return ({ counter: counter + 1 }); };
            },
            decrement: function (_a) {
                var counter = _a.counter;
                return function () { return ({ counter: counter - 1 }); };
            },
        });
        var p = P();
        expect(p.props.counter).toEqual(0);
        expect(typeof p.props.increment).toEqual('function');
        expect(typeof p.props.decrement).toEqual('function');
        p.props.increment();
        p.props.increment();
        expect(p.props.counter).toEqual(2);
        p.props.decrement();
        expect(p.props.counter).toEqual(1);
    });
});
//# sourceMappingURL=withStateHandlers.spec.js.map