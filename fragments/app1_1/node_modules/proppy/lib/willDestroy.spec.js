"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var compose_1 = require("./compose");
var withState_1 = require("./withState");
var willDestroy_1 = require("./willDestroy");
describe('proppy :: willDestroy', function () {
    test('is a function', function () {
        expect(typeof willDestroy_1.willDestroy).toEqual('function');
    });
    test('updates', function () {
        var called = false;
        var P = compose_1.compose(withState_1.withState('counter', 'setCounter', 0), willDestroy_1.willDestroy(function () {
            called = true;
        }));
        var p = P();
        p.subscribe(function () { });
        expect(p.props.counter).toEqual(0);
        expect(typeof p.props.setCounter).toEqual('function');
        expect(called).toBe(false);
        p.destroy();
        expect(called).toBe(true);
    });
});
//# sourceMappingURL=willDestroy.spec.js.map